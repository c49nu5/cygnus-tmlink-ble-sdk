// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v6.33.5
// source: Protos/cyg_tml_api_v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "Cygnus.BLE.Protobuf.V1";

/**
 * ---------------------------------------------------------------------------------------------------
 * ## CommandType lists all the commands that can be sent from Client to Gauge via WriteCommand characteristic.
 */
export enum CommandType {
  Non = 0,
  /** GetGaugeInfo - requests info about the Gauge (serial, model, variant, battery level etc). */
  GetGaugeInfo = 1,
  /** CancelRecordTransfer - send to cancel a Record or BScan transfer from the gauge. */
  CancelRecordTransfer = 2,
  /** GetRecordList - requests a list of Records on the gauge. */
  GetRecordList = 3,
  /** GetRecord - requests a Record from the gauge. */
  GetRecord = 4,
  /** GetRecordPoint - requests a Record's measurement point (No AScan data). */
  GetRecordPoint = 5,
  /** GetRecordPointAScan - requests a Record's measurement point (With AScan data). */
  GetRecordPointAScan = 6,
  /** DeleteRecord - requests a Record is deleted from the gauge. */
  DeleteRecord = 7,
  /** DeleteAllRecords - requests all Records are deleted from the gauge. */
  DeleteAllRecords = 8,
  /** NewRecord - sends a New blank Record to the Gauge. */
  NewRecord = 9,
  /** AddRecordPoints - sends a block of 1..10 blank Points and adds to the new Record on the Gauge. */
  AddRecordPoints = 10,
  /** GetBScanList - requests a list of BScans on the gauge. */
  GetBScanList = 11,
  /** GetBScan - requests a BScan from the gauge. */
  GetBScan = 12,
  /** GetBScanPoint - requests a BScan's measurement point (No AScan data). */
  GetBScanPoint = 13,
  /** GetBScanPointAScan - requests a BScan's measurement point (With AScan data). */
  GetBScanPointAScan = 14,
  /** DeleteBScan - requests a BScan is deleted from the gauge. */
  DeleteBScan = 15,
  /** DeleteAllBScans - requests all BScans are deleted from the gauge. */
  DeleteAllBScans = 16,
  UNRECOGNIZED = -1,
}

export function commandTypeFromJSON(object: any): CommandType {
  switch (object) {
    case 0:
    case "Non":
      return CommandType.Non;
    case 1:
    case "GetGaugeInfo":
      return CommandType.GetGaugeInfo;
    case 2:
    case "CancelRecordTransfer":
      return CommandType.CancelRecordTransfer;
    case 3:
    case "GetRecordList":
      return CommandType.GetRecordList;
    case 4:
    case "GetRecord":
      return CommandType.GetRecord;
    case 5:
    case "GetRecordPoint":
      return CommandType.GetRecordPoint;
    case 6:
    case "GetRecordPointAScan":
      return CommandType.GetRecordPointAScan;
    case 7:
    case "DeleteRecord":
      return CommandType.DeleteRecord;
    case 8:
    case "DeleteAllRecords":
      return CommandType.DeleteAllRecords;
    case 9:
    case "NewRecord":
      return CommandType.NewRecord;
    case 10:
    case "AddRecordPoints":
      return CommandType.AddRecordPoints;
    case 11:
    case "GetBScanList":
      return CommandType.GetBScanList;
    case 12:
    case "GetBScan":
      return CommandType.GetBScan;
    case 13:
    case "GetBScanPoint":
      return CommandType.GetBScanPoint;
    case 14:
    case "GetBScanPointAScan":
      return CommandType.GetBScanPointAScan;
    case 15:
    case "DeleteBScan":
      return CommandType.DeleteBScan;
    case 16:
    case "DeleteAllBScans":
      return CommandType.DeleteAllBScans;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CommandType.UNRECOGNIZED;
  }
}

export function commandTypeToJSON(object: CommandType): string {
  switch (object) {
    case CommandType.Non:
      return "Non";
    case CommandType.GetGaugeInfo:
      return "GetGaugeInfo";
    case CommandType.CancelRecordTransfer:
      return "CancelRecordTransfer";
    case CommandType.GetRecordList:
      return "GetRecordList";
    case CommandType.GetRecord:
      return "GetRecord";
    case CommandType.GetRecordPoint:
      return "GetRecordPoint";
    case CommandType.GetRecordPointAScan:
      return "GetRecordPointAScan";
    case CommandType.DeleteRecord:
      return "DeleteRecord";
    case CommandType.DeleteAllRecords:
      return "DeleteAllRecords";
    case CommandType.NewRecord:
      return "NewRecord";
    case CommandType.AddRecordPoints:
      return "AddRecordPoints";
    case CommandType.GetBScanList:
      return "GetBScanList";
    case CommandType.GetBScan:
      return "GetBScan";
    case CommandType.GetBScanPoint:
      return "GetBScanPoint";
    case CommandType.GetBScanPointAScan:
      return "GetBScanPointAScan";
    case CommandType.DeleteBScan:
      return "DeleteBScan";
    case CommandType.DeleteAllBScans:
      return "DeleteAllBScans";
    case CommandType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ErrorCodes {
  /** Success - Success, no error. */
  Success = 0,
  /** RecordNotFound - The Record was not found. */
  RecordNotFound = 1,
  /** BScanNotFound - The BScan was not found. */
  BScanNotFound = 2,
  /** InvalidParameter - An invalid parameter was received in a command. */
  InvalidParameter = 3,
  /** TransferNotStarted - Attempt to get Points before Header. */
  TransferNotStarted = 4,
  /** GzUncompressError - Gzip uncompress error. */
  GzUncompressError = 5,
  /** ProtoUnpackError - Protobuf unpack error. */
  ProtoUnpackError = 6,
  /** MemoryError - Gauge memory allocation error. */
  MemoryError = 7,
  /** RecordSaveError - Failed to save Record data. */
  RecordSaveError = 8,
  /** RecordNameError - Record name is invalid. */
  RecordNameError = 9,
  /** RecordZeroPoints - Attempt to created a record with zero points. */
  RecordZeroPoints = 10,
  /** RecordPointNumError - Attempt to created a record with too many points. */
  RecordPointNumError = 11,
  /** TransferCancelled - Record or BScan transfer cancelled. */
  TransferCancelled = 12,
  UNRECOGNIZED = -1,
}

export function errorCodesFromJSON(object: any): ErrorCodes {
  switch (object) {
    case 0:
    case "Success":
      return ErrorCodes.Success;
    case 1:
    case "RecordNotFound":
      return ErrorCodes.RecordNotFound;
    case 2:
    case "BScanNotFound":
      return ErrorCodes.BScanNotFound;
    case 3:
    case "InvalidParameter":
      return ErrorCodes.InvalidParameter;
    case 4:
    case "TransferNotStarted":
      return ErrorCodes.TransferNotStarted;
    case 5:
    case "GzUncompressError":
      return ErrorCodes.GzUncompressError;
    case 6:
    case "ProtoUnpackError":
      return ErrorCodes.ProtoUnpackError;
    case 7:
    case "MemoryError":
      return ErrorCodes.MemoryError;
    case 8:
    case "RecordSaveError":
      return ErrorCodes.RecordSaveError;
    case 9:
    case "RecordNameError":
      return ErrorCodes.RecordNameError;
    case 10:
    case "RecordZeroPoints":
      return ErrorCodes.RecordZeroPoints;
    case 11:
    case "RecordPointNumError":
      return ErrorCodes.RecordPointNumError;
    case 12:
    case "TransferCancelled":
      return ErrorCodes.TransferCancelled;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ErrorCodes.UNRECOGNIZED;
  }
}

export function errorCodesToJSON(object: ErrorCodes): string {
  switch (object) {
    case ErrorCodes.Success:
      return "Success";
    case ErrorCodes.RecordNotFound:
      return "RecordNotFound";
    case ErrorCodes.BScanNotFound:
      return "BScanNotFound";
    case ErrorCodes.InvalidParameter:
      return "InvalidParameter";
    case ErrorCodes.TransferNotStarted:
      return "TransferNotStarted";
    case ErrorCodes.GzUncompressError:
      return "GzUncompressError";
    case ErrorCodes.ProtoUnpackError:
      return "ProtoUnpackError";
    case ErrorCodes.MemoryError:
      return "MemoryError";
    case ErrorCodes.RecordSaveError:
      return "RecordSaveError";
    case ErrorCodes.RecordNameError:
      return "RecordNameError";
    case ErrorCodes.RecordZeroPoints:
      return "RecordZeroPoints";
    case ErrorCodes.RecordPointNumError:
      return "RecordPointNumError";
    case ErrorCodes.TransferCancelled:
      return "TransferCancelled";
    case ErrorCodes.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum LiveMeasurementType {
  Live = 0,
  Frozen = 1,
  UNRECOGNIZED = -1,
}

export function liveMeasurementTypeFromJSON(object: any): LiveMeasurementType {
  switch (object) {
    case 0:
    case "Live":
      return LiveMeasurementType.Live;
    case 1:
    case "Frozen":
      return LiveMeasurementType.Frozen;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LiveMeasurementType.UNRECOGNIZED;
  }
}

export function liveMeasurementTypeToJSON(object: LiveMeasurementType): string {
  switch (object) {
    case LiveMeasurementType.Live:
      return "Live";
    case LiveMeasurementType.Frozen:
      return "Frozen";
    case LiveMeasurementType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RecordType {
  /** Linear - Measurements taken in a 1D list (use xColumns only). */
  Linear = 0,
  /** Grid - Measurements taken in a 2D grid. */
  Grid = 1,
  UNRECOGNIZED = -1,
}

export function recordTypeFromJSON(object: any): RecordType {
  switch (object) {
    case 0:
    case "Linear":
      return RecordType.Linear;
    case 1:
    case "Grid":
      return RecordType.Grid;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RecordType.UNRECOGNIZED;
  }
}

export function recordTypeToJSON(object: RecordType): string {
  switch (object) {
    case RecordType.Linear:
      return "Linear";
    case RecordType.Grid:
      return "Grid";
    case RecordType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * The GridPattern must be specified when creating new Records whose RecordType is Grid.
 * This dictates the start and sequence of logging in the grid.
 */
export enum GridPattern {
  /** RDRD - RightDown, RightDown.. */
  RDRD = 0,
  /** LDLD - LeftDown, LeftDown.. */
  LDLD = 1,
  /** RURU - RightUp, RightUp.. */
  RURU = 2,
  LULU = 3,
  DRDR = 4,
  DLDL = 5,
  URUR = 6,
  ULUL = 7,
  RDLD = 8,
  LDRD = 9,
  RULU = 10,
  LURU = 11,
  URDR = 12,
  ULDL = 13,
  DRUR = 14,
  DLUL = 15,
  UNRECOGNIZED = -1,
}

export function gridPatternFromJSON(object: any): GridPattern {
  switch (object) {
    case 0:
    case "RDRD":
      return GridPattern.RDRD;
    case 1:
    case "LDLD":
      return GridPattern.LDLD;
    case 2:
    case "RURU":
      return GridPattern.RURU;
    case 3:
    case "LULU":
      return GridPattern.LULU;
    case 4:
    case "DRDR":
      return GridPattern.DRDR;
    case 5:
    case "DLDL":
      return GridPattern.DLDL;
    case 6:
    case "URUR":
      return GridPattern.URUR;
    case 7:
    case "ULUL":
      return GridPattern.ULUL;
    case 8:
    case "RDLD":
      return GridPattern.RDLD;
    case 9:
    case "LDRD":
      return GridPattern.LDRD;
    case 10:
    case "RULU":
      return GridPattern.RULU;
    case 11:
    case "LURU":
      return GridPattern.LURU;
    case 12:
    case "URDR":
      return GridPattern.URDR;
    case 13:
    case "ULDL":
      return GridPattern.ULDL;
    case 14:
    case "DRUR":
      return GridPattern.DRUR;
    case 15:
    case "DLUL":
      return GridPattern.DLUL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GridPattern.UNRECOGNIZED;
  }
}

export function gridPatternToJSON(object: GridPattern): string {
  switch (object) {
    case GridPattern.RDRD:
      return "RDRD";
    case GridPattern.LDLD:
      return "LDLD";
    case GridPattern.RURU:
      return "RURU";
    case GridPattern.LULU:
      return "LULU";
    case GridPattern.DRDR:
      return "DRDR";
    case GridPattern.DLDL:
      return "DLDL";
    case GridPattern.URUR:
      return "URUR";
    case GridPattern.ULUL:
      return "ULUL";
    case GridPattern.RDLD:
      return "RDLD";
    case GridPattern.LDRD:
      return "LDRD";
    case GridPattern.RULU:
      return "RULU";
    case GridPattern.LURU:
      return "LURU";
    case GridPattern.URDR:
      return "URDR";
    case GridPattern.ULDL:
      return "ULDL";
    case GridPattern.DRUR:
      return "DRUR";
    case GridPattern.DLUL:
      return "DLUL";
    case GridPattern.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Method {
  /** Spot - Spot measurement. */
  Spot = 0,
  /** Scan - Scan measurement. */
  Scan = 1,
  UNRECOGNIZED = -1,
}

export function methodFromJSON(object: any): Method {
  switch (object) {
    case 0:
    case "Spot":
      return Method.Spot;
    case 1:
    case "Scan":
      return Method.Scan;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Method.UNRECOGNIZED;
  }
}

export function methodToJSON(object: Method): string {
  switch (object) {
    case Method.Spot:
      return "Spot";
    case Method.Scan:
      return "Scan";
    case Method.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ProbeType {
  /** Single - Single crystal probe, direct contact. */
  Single = 0,
  /** Dual - Dual crystal probe, with delay line. */
  Dual = 1,
  UNRECOGNIZED = -1,
}

export function probeTypeFromJSON(object: any): ProbeType {
  switch (object) {
    case 0:
    case "Single":
      return ProbeType.Single;
    case 1:
    case "Dual":
      return ProbeType.Dual;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProbeType.UNRECOGNIZED;
  }
}

export function probeTypeToJSON(object: ProbeType): string {
  switch (object) {
    case ProbeType.Single:
      return "Single";
    case ProbeType.Dual:
      return "Dual";
    case ProbeType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum UOM {
  /** Metric - Measurements are in micro-meters (m/10e6), velocity is in m/s. */
  Metric = 0,
  /** Imperial - Measurements are in thou's or milli-inches (in/10e3), velocity is in 10e4*inches/micro-second. */
  Imperial = 1,
  UNRECOGNIZED = -1,
}

export function uOMFromJSON(object: any): UOM {
  switch (object) {
    case 0:
    case "Metric":
      return UOM.Metric;
    case 1:
    case "Imperial":
      return UOM.Imperial;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UOM.UNRECOGNIZED;
  }
}

export function uOMToJSON(object: UOM): string {
  switch (object) {
    case UOM.Metric:
      return "Metric";
    case UOM.Imperial:
      return "Imperial";
    case UOM.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum UTMode {
  /** SE - UT Mode 2 = Interface to First Back-wall Echo. */
  SE = 0,
  /** EE - UT Mode 3 = Two Back-wall Echoes. */
  EE = 1,
  /** ME - UT Mode 3 = Three Back-wall Echoes. */
  ME = 2,
  /** GT - Manual Gates Used. */
  GT = 3,
  UNRECOGNIZED = -1,
}

export function uTModeFromJSON(object: any): UTMode {
  switch (object) {
    case 0:
    case "SE":
      return UTMode.SE;
    case 1:
    case "EE":
      return UTMode.EE;
    case 2:
    case "ME":
      return UTMode.ME;
    case 3:
    case "GT":
      return UTMode.GT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UTMode.UNRECOGNIZED;
  }
}

export function uTModeToJSON(object: UTMode): string {
  switch (object) {
    case UTMode.SE:
      return "SE";
    case UTMode.EE:
      return "EE";
    case UTMode.ME:
      return "ME";
    case UTMode.GT:
      return "GT";
    case UTMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AScanRectify {
  /** RF - No rectification, RF signal ranging from -120 to +120%. */
  RF = 0,
  /** FW - Full Wave rectified, signal ranging from 0 to +120%. */
  FW = 1,
  /** PHW - Positive Half Wave rectified, signal ranging from 0 to +120%. */
  PHW = 2,
  /** NHW - Negative Half Wave rectified, signal ranging from 0 to +120%. */
  NHW = 3,
  UNRECOGNIZED = -1,
}

export function aScanRectifyFromJSON(object: any): AScanRectify {
  switch (object) {
    case 0:
    case "RF":
      return AScanRectify.RF;
    case 1:
    case "FW":
      return AScanRectify.FW;
    case 2:
    case "PHW":
      return AScanRectify.PHW;
    case 3:
    case "NHW":
      return AScanRectify.NHW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AScanRectify.UNRECOGNIZED;
  }
}

export function aScanRectifyToJSON(object: AScanRectify): string {
  switch (object) {
    case AScanRectify.RF:
      return "RF";
    case AScanRectify.FW:
      return "FW";
    case AScanRectify.PHW:
      return "PHW";
    case AScanRectify.NHW:
      return "NHW";
    case AScanRectify.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum GaugeVariant {
  Test = 0,
  Basic_SC = 1,
  Basic_TC = 2,
  Plus = 3,
  Pro = 4,
  UNRECOGNIZED = -1,
}

export function gaugeVariantFromJSON(object: any): GaugeVariant {
  switch (object) {
    case 0:
    case "Test":
      return GaugeVariant.Test;
    case 1:
    case "Basic_SC":
      return GaugeVariant.Basic_SC;
    case 2:
    case "Basic_TC":
      return GaugeVariant.Basic_TC;
    case 3:
    case "Plus":
      return GaugeVariant.Plus;
    case 4:
    case "Pro":
      return GaugeVariant.Pro;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GaugeVariant.UNRECOGNIZED;
  }
}

export function gaugeVariantToJSON(object: GaugeVariant): string {
  switch (object) {
    case GaugeVariant.Test:
      return "Test";
    case GaugeVariant.Basic_SC:
      return "Basic_SC";
    case GaugeVariant.Basic_TC:
      return "Basic_TC";
    case GaugeVariant.Plus:
      return "Plus";
    case GaugeVariant.Pro:
      return "Pro";
    case GaugeVariant.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MeasurementPointState {
  /** Blank - The measurement is blank. */
  Blank = 0,
  /** Valid - The measurement value is valid. */
  Valid = 1,
  /** Deleted - The measurement has been deleted by the gauge user. */
  Deleted = 2,
  /** Obstructed - The measurement position could not be accessed. */
  Obstructed = 3,
  /** NoReading - No measurement could be obtained. */
  NoReading = 4,
  UNRECOGNIZED = -1,
}

export function measurementPointStateFromJSON(object: any): MeasurementPointState {
  switch (object) {
    case 0:
    case "Blank":
      return MeasurementPointState.Blank;
    case 1:
    case "Valid":
      return MeasurementPointState.Valid;
    case 2:
    case "Deleted":
      return MeasurementPointState.Deleted;
    case 3:
    case "Obstructed":
      return MeasurementPointState.Obstructed;
    case 4:
    case "NoReading":
      return MeasurementPointState.NoReading;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MeasurementPointState.UNRECOGNIZED;
  }
}

export function measurementPointStateToJSON(object: MeasurementPointState): string {
  switch (object) {
    case MeasurementPointState.Blank:
      return "Blank";
    case MeasurementPointState.Valid:
      return "Valid";
    case MeasurementPointState.Deleted:
      return "Deleted";
    case MeasurementPointState.Obstructed:
      return "Obstructed";
    case MeasurementPointState.NoReading:
      return "NoReading";
    case MeasurementPointState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------------------------------
 * ## NotifyMessage is used to notify the Client that a new Message is ready to read.
 * Used for the BLE Service 'NotifyMessageReady' characteristic.
 * NotifyMessage messages are never gzip compressed.
 */
export interface NotifyMessage {
  /** The command that triggered this message. */
  commandType: CommandType;
  /** Error code. */
  errorCode: ErrorCodes;
  /** True if data for the client to read. */
  readDataAvailable: boolean;
}

/**
 * ---------------------------------------------------------------------------------------------------
 * ## Command contains the Command send from the Client to the gauge.
 * Used for the BLE Service 'WriteCommand' Write characteristic.
 * Command messages are always gzip compressed.
 */
export interface Command {
  /** The command type being sent in the message. */
  commandType: CommandType;
  /** Sets Gauge date-time when non-zero is sent. */
  timestamp:
    | Date
    | undefined;
  /** Optional name of a Record or BScan. (30 chars max). */
  name: string;
  /** Optional newRecord message. */
  newRecord:
    | Command_NewRecord
    | undefined;
  /** Optional addRecordPoints message. */
  addRecordPoints: Command_AddRecordPoints | undefined;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Used to create a new Record in the gauge.
 */
export interface Command_NewRecord {
  /** record name or ID (30 chars max). */
  name: string;
  /** TML Key or ID  (30 chars max). */
  key: string;
  /** Linear or Grid */
  recordType: RecordType;
  /** Grid Pattern (if Grid type) */
  gridPattern: GridPattern;
  /** Units of Measurement. */
  uom: UOM;
  /** Linear or Grid X Dimension 1..N. */
  numColsX: number;
  /** Grid Y coordinate 1..N. */
  numRowsY: number;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Used to add blank points to a new record.
 * Send in blocks of 1..10 points.
 * If more than 10 points are sent, the message will be discarded and a NAK response returned.
 */
export interface Command_AddRecordPoints {
  /** record name or ID (30 chars max). */
  name: string;
  /** Array of 1..10 measurement points per message. */
  mpoints: Command_AddRecordPoints_MPoint[];
}

/** --------------------------------------------------------------- */
export interface Command_AddRecordPoints_MPoint {
  /** point name (30 chars max). */
  name: string;
  /** TML Key or ID number. */
  key: number;
  /** Method; Spot or Scan. */
  method: Method;
  /** Linear or Grid X Coordinate, 0 based. */
  colNumX: number;
  /** Grid Y coordinate, 0 based. */
  rowNumY: number;
  /** Minimum thickness warning (in UoM) or zero if not required. */
  minThickness: number;
  /** Maximum thickness warning (in UoM) or zero if not required. */
  maxThickness: number;
}

/**
 * ---------------------------------------------------------------------------------------------------
 * ## Message contains the message sent from the gauge to the Client in response to a received Command.
 * Example, for a GetRecodList command, the reply would be [Message] with [RecordList] data.
 * Used for the BLE Service 'ReadMessage' Read Characteristic.
 * Message messages are always gzip compressed.
 */
export interface Message {
  /** The command that triggered this message. */
  commandType: CommandType;
  /** An optional GaugeInfo message. */
  gaugeInfo:
    | Message_GaugeInfo
    | undefined;
  /** An optional Record List. */
  recordList:
    | Message_RecordList
    | undefined;
  /** An optional Record. */
  record:
    | Message_Record
    | undefined;
  /** An optional RecordPoint. */
  recordPoint:
    | Message_RecordPoint
    | undefined;
  /** An optional BScan List. */
  bscanList:
    | Message_BScanList
    | undefined;
  /** An optional BScan. */
  bscan:
    | Message_BScan
    | undefined;
  /** An optional BScanPoint. */
  bscanPoint: Message_BScanPoint | undefined;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains info about the Gauge.
 */
export interface Message_GaugeInfo {
  /** Gauge serial number. */
  serialNumber: number;
  /** Gauge ID number. */
  gaugeUD: number;
  /** Version number as Major.Minor.Build [U8,U8,U16] */
  versionNumber: number;
  /** Battery level in percent. */
  batteryLevel: number;
  /** Gauge variant enum. */
  gaugeVariant: GaugeVariant;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains a list of Record found on the gauge.
 */
export interface Message_RecordList {
  /** Array of 0..N record items. */
  items: Message_RecordList_Item[];
}

/** --------------------------------------------------------------- */
export interface Message_RecordList_Item {
  /** record name or ID (30 chars max). */
  name: string;
  /** TML Key or ID  (30 chars max). */
  key: string;
  /** size of the record file in bytes. */
  fileSize: number;
  /** timestamp when record file created. */
  created:
    | Date
    | undefined;
  /** timestamp when record file last saved. */
  updated:
    | Date
    | undefined;
  /** type - Linear, Grid. */
  recordType: RecordType;
  /** number of measurement points required. */
  numPointsRequired: number;
  /** number of measurement points taken. */
  numPointsTaken: number;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains record 'header' data.
 * Any measurement points will follow in subsequent messages.
 */
export interface Message_Record {
  /** RecordID. */
  recordID: number;
  /** record name or ID (30 chars max). */
  name: string;
  /** location name or ID (16 chars max). */
  location: string;
  /** surveyor name or ID (16 chars max). */
  surveyor: string;
  /** TML Key or ID (30 chars max). */
  key: string;
  /** timestamp when record file created. */
  created:
    | Date
    | undefined;
  /** timestamp when record file last saved. */
  updated: Date | undefined;
  recordType: RecordType;
  numPointsRequired: number;
  /** The number of RecordPoint messages to read. */
  numPointsTaken: number;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains a single Measurement Point belonging to a Record.
 */
export interface Message_RecordPoint {
  /** Links this MPoint to a Record. */
  recordID: number;
  /** Point Name (30 chars max). */
  name: string;
  /** TML Key or ID value. */
  key: number;
  /** The measurement state. */
  state: MeasurementPointState;
  /** Method; Spot or Scan. */
  method: Method;
  /** Units of Measurement. */
  uom: UOM;
  /** Thickness value in UOM. */
  thickness: number;
  /** Velocity of sound in UOM. */
  velocity: number;
  /** Probe type; Single, Dual. */
  probeType: ProbeType;
  /** UT Measurement Mode; SE/EE/ME. */
  UTMode: UTMode;
  /** Measurement taken timestamp. */
  taken:
    | Date
    | undefined;
  /** Linear or Grid X Coordinate, 0 based. */
  colNumX: number;
  /** Grid Y coordinate, 0 based. */
  rowNumY: number;
  /** optional AScan Data. */
  ascan: AScan | undefined;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains a list of BScan found on the gauge.
 */
export interface Message_BScanList {
  /** Array of 0..N record items. */
  items: Message_BScanList_Item[];
}

/** --------------------------------------------------------------- */
export interface Message_BScanList_Item {
  /** record name or ID (30 chars max). */
  name: string;
  /** TML Key or ID  (30 chars max). */
  key: string;
  /** size of the bscan file in bytes. */
  fileSize: number;
  /** timestamp bscan file last saved. */
  updated:
    | Date
    | undefined;
  /** number of measurement points in the scan. */
  numScanPoints: number;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains BScan 'header' data.
 * Any scan points will follow in subsequent messages.
 */
export interface Message_BScan {
  /** BScanID. */
  BScanID: number;
  /** BScan name or ID (30 chars max). */
  name: string;
  /** TML Key or ID (30 chars max). */
  key: string;
  /** record last save timestamp. */
  updated:
    | Date
    | undefined;
  /** number of measurement points in the scan. */
  numScanPoints: number;
  /** Scan internal in ms. */
  scanInterval: number;
}

/**
 * ------------------------------------------------------------------------------------------------
 * Contains a single Measurement Point belonging to a BScan.
 */
export interface Message_BScanPoint {
  /** Links this MPoint to a BScan. */
  BScanID: number;
  /** Units of Measurement. */
  uom: UOM;
  /** Thickness value in UOM. */
  thickness: number;
  /** Velocity of sound in UOM. */
  velocity: number;
  /** Probe type; Single, Dual. */
  probeType: ProbeType;
  /** UT Measurement Mode; SE/EE/ME. */
  UTMode: UTMode;
  /** Point Number in scan, 0 based. */
  scanPointNum: number;
  /** optional AScan Data. */
  ascan: AScan | undefined;
}

/**
 * ---------------------------------------------------------------------------------------------------
 * ## A live measurement notification message.
 * NotifyLiveMeasurement messages are never gzip compressed.
 */
export interface NotifyLiveMeasurement {
  /** Informs of the Live Measurement type. */
  liveMeasurementType: LiveMeasurementType;
  /** If liveMeasurementType == Live these items are included.. */
  index: number;
  /** Status Bits x 5 [DEEPCOAT, INCHES, FROZEN, STABLE, VALID](LSB). */
  statusBits: number;
  /** UT Measurement Mode; SE/EE/ME. */
  UTMode: UTMode;
  /** Thickness value in UOM. */
  thickness: number;
  /** Velocity of sound in UOM. */
  velocity: number;
  /** Gain set in dB */
  gaindB: number;
  /** Surface temp in C if High Temp mode, else zero. */
  surfaceTemp: number;
  /** Battery level in percent. */
  batteryLevel: number;
}

/**
 * ------------------------------------------------------------------------------------------------
 * ## Contains a single Frozen Live Measurement.
 * FrozenLiveMeasurement messages are always gzip compressed.
 */
export interface FrozenLiveMeasurement {
  /** Measurement index number. */
  index: number;
  /** Gauge serial number. */
  serialNumber: number;
  /** Gauge ID number. */
  gaugeUD: number;
  /** Battery level in percent. */
  batteryLevel: number;
  /** True when valid measurement. */
  validMeasurement: boolean;
  /** True when stable measurement. */
  stableMeasurement: boolean;
  /** Units of Measurement. */
  uom: UOM;
  /** Thickness value in UOM. */
  thickness: number;
  /** Velocity of sound in UOM. */
  velocity: number;
  /** Probe type; Single, Dual. */
  probeType: ProbeType;
  /** UT Measurement Mode; SE/EE/ME. */
  UTMode: UTMode;
  /** Gain set in dB */
  gaindB: number;
  /** Surface temp In C if High Temp mode, else zero. */
  surfaceTemp: number;
  /** AScan data. */
  ascan: AScan | undefined;
}

/**
 * ------------------------------------------------------------------------------------------------
 * ## AScan Graph Data. Used by Messages and LiveMeasurements.
 * AScan messages are always gzip compressed.
 */
export interface AScan {
  /** AScan graph rectification mode used. */
  rectify: AScanRectify;
  /** AScan graph start in UoM. */
  ascanStart: number;
  /** AScan graph width in UoM. */
  ascanWidth: number;
  /** AScan graph data points in %, from -120 to +120. */
  ascanPoints: Uint8Array;
  /** Echo Marker for 1st Detected Echo in UoM (0=none). */
  echo1: number;
  /** Echo Marker for 2nd Detected Echo in UoM (0=none). */
  echo2: number;
  /** Echo Marker for 3rd Detected Echo in UoM (0=none). */
  echo3: number;
}

function createBaseNotifyMessage(): NotifyMessage {
  return { commandType: 0, errorCode: 0, readDataAvailable: false };
}

export const NotifyMessage: MessageFns<NotifyMessage> = {
  encode(message: NotifyMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandType !== 0) {
      writer.uint32(8).int32(message.commandType);
    }
    if (message.errorCode !== 0) {
      writer.uint32(16).int32(message.errorCode);
    }
    if (message.readDataAvailable !== false) {
      writer.uint32(24).bool(message.readDataAvailable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotifyMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotifyMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.commandType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.errorCode = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.readDataAvailable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotifyMessage {
    return {
      commandType: isSet(object.commandType) ? commandTypeFromJSON(object.commandType) : 0,
      errorCode: isSet(object.errorCode) ? errorCodesFromJSON(object.errorCode) : 0,
      readDataAvailable: isSet(object.readDataAvailable) ? globalThis.Boolean(object.readDataAvailable) : false,
    };
  },

  toJSON(message: NotifyMessage): unknown {
    const obj: any = {};
    if (message.commandType !== 0) {
      obj.commandType = commandTypeToJSON(message.commandType);
    }
    if (message.errorCode !== 0) {
      obj.errorCode = errorCodesToJSON(message.errorCode);
    }
    if (message.readDataAvailable !== false) {
      obj.readDataAvailable = message.readDataAvailable;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotifyMessage>, I>>(base?: I): NotifyMessage {
    return NotifyMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotifyMessage>, I>>(object: I): NotifyMessage {
    const message = createBaseNotifyMessage();
    message.commandType = object.commandType ?? 0;
    message.errorCode = object.errorCode ?? 0;
    message.readDataAvailable = object.readDataAvailable ?? false;
    return message;
  },
};

function createBaseCommand(): Command {
  return { commandType: 0, timestamp: undefined, name: "", newRecord: undefined, addRecordPoints: undefined };
}

export const Command: MessageFns<Command> = {
  encode(message: Command, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandType !== 0) {
      writer.uint32(8).int32(message.commandType);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(18).fork()).join();
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.newRecord !== undefined) {
      Command_NewRecord.encode(message.newRecord, writer.uint32(34).fork()).join();
    }
    if (message.addRecordPoints !== undefined) {
      Command_AddRecordPoints.encode(message.addRecordPoints, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Command {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.commandType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.newRecord = Command_NewRecord.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.addRecordPoints = Command_AddRecordPoints.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Command {
    return {
      commandType: isSet(object.commandType) ? commandTypeFromJSON(object.commandType) : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      newRecord: isSet(object.newRecord) ? Command_NewRecord.fromJSON(object.newRecord) : undefined,
      addRecordPoints: isSet(object.addRecordPoints)
        ? Command_AddRecordPoints.fromJSON(object.addRecordPoints)
        : undefined,
    };
  },

  toJSON(message: Command): unknown {
    const obj: any = {};
    if (message.commandType !== 0) {
      obj.commandType = commandTypeToJSON(message.commandType);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.newRecord !== undefined) {
      obj.newRecord = Command_NewRecord.toJSON(message.newRecord);
    }
    if (message.addRecordPoints !== undefined) {
      obj.addRecordPoints = Command_AddRecordPoints.toJSON(message.addRecordPoints);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Command>, I>>(base?: I): Command {
    return Command.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Command>, I>>(object: I): Command {
    const message = createBaseCommand();
    message.commandType = object.commandType ?? 0;
    message.timestamp = object.timestamp ?? undefined;
    message.name = object.name ?? "";
    message.newRecord = (object.newRecord !== undefined && object.newRecord !== null)
      ? Command_NewRecord.fromPartial(object.newRecord)
      : undefined;
    message.addRecordPoints = (object.addRecordPoints !== undefined && object.addRecordPoints !== null)
      ? Command_AddRecordPoints.fromPartial(object.addRecordPoints)
      : undefined;
    return message;
  },
};

function createBaseCommand_NewRecord(): Command_NewRecord {
  return { name: "", key: "", recordType: 0, gridPattern: 0, uom: 0, numColsX: 0, numRowsY: 0 };
}

export const Command_NewRecord: MessageFns<Command_NewRecord> = {
  encode(message: Command_NewRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.recordType !== 0) {
      writer.uint32(24).int32(message.recordType);
    }
    if (message.gridPattern !== 0) {
      writer.uint32(32).int32(message.gridPattern);
    }
    if (message.uom !== 0) {
      writer.uint32(40).int32(message.uom);
    }
    if (message.numColsX !== 0) {
      writer.uint32(48).uint32(message.numColsX);
    }
    if (message.numRowsY !== 0) {
      writer.uint32(56).uint32(message.numRowsY);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Command_NewRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommand_NewRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.recordType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.gridPattern = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.uom = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.numColsX = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.numRowsY = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Command_NewRecord {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      recordType: isSet(object.recordType) ? recordTypeFromJSON(object.recordType) : 0,
      gridPattern: isSet(object.gridPattern) ? gridPatternFromJSON(object.gridPattern) : 0,
      uom: isSet(object.uom) ? uOMFromJSON(object.uom) : 0,
      numColsX: isSet(object.numColsX) ? globalThis.Number(object.numColsX) : 0,
      numRowsY: isSet(object.numRowsY) ? globalThis.Number(object.numRowsY) : 0,
    };
  },

  toJSON(message: Command_NewRecord): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.recordType !== 0) {
      obj.recordType = recordTypeToJSON(message.recordType);
    }
    if (message.gridPattern !== 0) {
      obj.gridPattern = gridPatternToJSON(message.gridPattern);
    }
    if (message.uom !== 0) {
      obj.uom = uOMToJSON(message.uom);
    }
    if (message.numColsX !== 0) {
      obj.numColsX = Math.round(message.numColsX);
    }
    if (message.numRowsY !== 0) {
      obj.numRowsY = Math.round(message.numRowsY);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Command_NewRecord>, I>>(base?: I): Command_NewRecord {
    return Command_NewRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Command_NewRecord>, I>>(object: I): Command_NewRecord {
    const message = createBaseCommand_NewRecord();
    message.name = object.name ?? "";
    message.key = object.key ?? "";
    message.recordType = object.recordType ?? 0;
    message.gridPattern = object.gridPattern ?? 0;
    message.uom = object.uom ?? 0;
    message.numColsX = object.numColsX ?? 0;
    message.numRowsY = object.numRowsY ?? 0;
    return message;
  },
};

function createBaseCommand_AddRecordPoints(): Command_AddRecordPoints {
  return { name: "", mpoints: [] };
}

export const Command_AddRecordPoints: MessageFns<Command_AddRecordPoints> = {
  encode(message: Command_AddRecordPoints, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.mpoints) {
      Command_AddRecordPoints_MPoint.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Command_AddRecordPoints {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommand_AddRecordPoints();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mpoints.push(Command_AddRecordPoints_MPoint.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Command_AddRecordPoints {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      mpoints: globalThis.Array.isArray(object?.mpoints)
        ? object.mpoints.map((e: any) => Command_AddRecordPoints_MPoint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Command_AddRecordPoints): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.mpoints?.length) {
      obj.mpoints = message.mpoints.map((e) => Command_AddRecordPoints_MPoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Command_AddRecordPoints>, I>>(base?: I): Command_AddRecordPoints {
    return Command_AddRecordPoints.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Command_AddRecordPoints>, I>>(object: I): Command_AddRecordPoints {
    const message = createBaseCommand_AddRecordPoints();
    message.name = object.name ?? "";
    message.mpoints = object.mpoints?.map((e) => Command_AddRecordPoints_MPoint.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCommand_AddRecordPoints_MPoint(): Command_AddRecordPoints_MPoint {
  return { name: "", key: 0, method: 0, colNumX: 0, rowNumY: 0, minThickness: 0, maxThickness: 0 };
}

export const Command_AddRecordPoints_MPoint: MessageFns<Command_AddRecordPoints_MPoint> = {
  encode(message: Command_AddRecordPoints_MPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.key !== 0) {
      writer.uint32(16).uint32(message.key);
    }
    if (message.method !== 0) {
      writer.uint32(24).int32(message.method);
    }
    if (message.colNumX !== 0) {
      writer.uint32(32).uint32(message.colNumX);
    }
    if (message.rowNumY !== 0) {
      writer.uint32(40).uint32(message.rowNumY);
    }
    if (message.minThickness !== 0) {
      writer.uint32(48).uint32(message.minThickness);
    }
    if (message.maxThickness !== 0) {
      writer.uint32(56).uint32(message.maxThickness);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Command_AddRecordPoints_MPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommand_AddRecordPoints_MPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.method = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.colNumX = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rowNumY = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.minThickness = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxThickness = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Command_AddRecordPoints_MPoint {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      method: isSet(object.method) ? methodFromJSON(object.method) : 0,
      colNumX: isSet(object.colNumX) ? globalThis.Number(object.colNumX) : 0,
      rowNumY: isSet(object.rowNumY) ? globalThis.Number(object.rowNumY) : 0,
      minThickness: isSet(object.minThickness) ? globalThis.Number(object.minThickness) : 0,
      maxThickness: isSet(object.maxThickness) ? globalThis.Number(object.maxThickness) : 0,
    };
  },

  toJSON(message: Command_AddRecordPoints_MPoint): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.method !== 0) {
      obj.method = methodToJSON(message.method);
    }
    if (message.colNumX !== 0) {
      obj.colNumX = Math.round(message.colNumX);
    }
    if (message.rowNumY !== 0) {
      obj.rowNumY = Math.round(message.rowNumY);
    }
    if (message.minThickness !== 0) {
      obj.minThickness = Math.round(message.minThickness);
    }
    if (message.maxThickness !== 0) {
      obj.maxThickness = Math.round(message.maxThickness);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Command_AddRecordPoints_MPoint>, I>>(base?: I): Command_AddRecordPoints_MPoint {
    return Command_AddRecordPoints_MPoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Command_AddRecordPoints_MPoint>, I>>(
    object: I,
  ): Command_AddRecordPoints_MPoint {
    const message = createBaseCommand_AddRecordPoints_MPoint();
    message.name = object.name ?? "";
    message.key = object.key ?? 0;
    message.method = object.method ?? 0;
    message.colNumX = object.colNumX ?? 0;
    message.rowNumY = object.rowNumY ?? 0;
    message.minThickness = object.minThickness ?? 0;
    message.maxThickness = object.maxThickness ?? 0;
    return message;
  },
};

function createBaseMessage(): Message {
  return {
    commandType: 0,
    gaugeInfo: undefined,
    recordList: undefined,
    record: undefined,
    recordPoint: undefined,
    bscanList: undefined,
    bscan: undefined,
    bscanPoint: undefined,
  };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandType !== 0) {
      writer.uint32(8).int32(message.commandType);
    }
    if (message.gaugeInfo !== undefined) {
      Message_GaugeInfo.encode(message.gaugeInfo, writer.uint32(18).fork()).join();
    }
    if (message.recordList !== undefined) {
      Message_RecordList.encode(message.recordList, writer.uint32(26).fork()).join();
    }
    if (message.record !== undefined) {
      Message_Record.encode(message.record, writer.uint32(34).fork()).join();
    }
    if (message.recordPoint !== undefined) {
      Message_RecordPoint.encode(message.recordPoint, writer.uint32(42).fork()).join();
    }
    if (message.bscanList !== undefined) {
      Message_BScanList.encode(message.bscanList, writer.uint32(50).fork()).join();
    }
    if (message.bscan !== undefined) {
      Message_BScan.encode(message.bscan, writer.uint32(58).fork()).join();
    }
    if (message.bscanPoint !== undefined) {
      Message_BScanPoint.encode(message.bscanPoint, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.commandType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gaugeInfo = Message_GaugeInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recordList = Message_RecordList.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.record = Message_Record.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.recordPoint = Message_RecordPoint.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.bscanList = Message_BScanList.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.bscan = Message_BScan.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.bscanPoint = Message_BScanPoint.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return {
      commandType: isSet(object.commandType) ? commandTypeFromJSON(object.commandType) : 0,
      gaugeInfo: isSet(object.gaugeInfo) ? Message_GaugeInfo.fromJSON(object.gaugeInfo) : undefined,
      recordList: isSet(object.recordList) ? Message_RecordList.fromJSON(object.recordList) : undefined,
      record: isSet(object.record) ? Message_Record.fromJSON(object.record) : undefined,
      recordPoint: isSet(object.recordPoint) ? Message_RecordPoint.fromJSON(object.recordPoint) : undefined,
      bscanList: isSet(object.bscanList) ? Message_BScanList.fromJSON(object.bscanList) : undefined,
      bscan: isSet(object.bscan) ? Message_BScan.fromJSON(object.bscan) : undefined,
      bscanPoint: isSet(object.bscanPoint) ? Message_BScanPoint.fromJSON(object.bscanPoint) : undefined,
    };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.commandType !== 0) {
      obj.commandType = commandTypeToJSON(message.commandType);
    }
    if (message.gaugeInfo !== undefined) {
      obj.gaugeInfo = Message_GaugeInfo.toJSON(message.gaugeInfo);
    }
    if (message.recordList !== undefined) {
      obj.recordList = Message_RecordList.toJSON(message.recordList);
    }
    if (message.record !== undefined) {
      obj.record = Message_Record.toJSON(message.record);
    }
    if (message.recordPoint !== undefined) {
      obj.recordPoint = Message_RecordPoint.toJSON(message.recordPoint);
    }
    if (message.bscanList !== undefined) {
      obj.bscanList = Message_BScanList.toJSON(message.bscanList);
    }
    if (message.bscan !== undefined) {
      obj.bscan = Message_BScan.toJSON(message.bscan);
    }
    if (message.bscanPoint !== undefined) {
      obj.bscanPoint = Message_BScanPoint.toJSON(message.bscanPoint);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message>, I>>(base?: I): Message {
    return Message.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message>, I>>(object: I): Message {
    const message = createBaseMessage();
    message.commandType = object.commandType ?? 0;
    message.gaugeInfo = (object.gaugeInfo !== undefined && object.gaugeInfo !== null)
      ? Message_GaugeInfo.fromPartial(object.gaugeInfo)
      : undefined;
    message.recordList = (object.recordList !== undefined && object.recordList !== null)
      ? Message_RecordList.fromPartial(object.recordList)
      : undefined;
    message.record = (object.record !== undefined && object.record !== null)
      ? Message_Record.fromPartial(object.record)
      : undefined;
    message.recordPoint = (object.recordPoint !== undefined && object.recordPoint !== null)
      ? Message_RecordPoint.fromPartial(object.recordPoint)
      : undefined;
    message.bscanList = (object.bscanList !== undefined && object.bscanList !== null)
      ? Message_BScanList.fromPartial(object.bscanList)
      : undefined;
    message.bscan = (object.bscan !== undefined && object.bscan !== null)
      ? Message_BScan.fromPartial(object.bscan)
      : undefined;
    message.bscanPoint = (object.bscanPoint !== undefined && object.bscanPoint !== null)
      ? Message_BScanPoint.fromPartial(object.bscanPoint)
      : undefined;
    return message;
  },
};

function createBaseMessage_GaugeInfo(): Message_GaugeInfo {
  return { serialNumber: 0, gaugeUD: 0, versionNumber: 0, batteryLevel: 0, gaugeVariant: 0 };
}

export const Message_GaugeInfo: MessageFns<Message_GaugeInfo> = {
  encode(message: Message_GaugeInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serialNumber !== 0) {
      writer.uint32(8).uint32(message.serialNumber);
    }
    if (message.gaugeUD !== 0) {
      writer.uint32(16).uint32(message.gaugeUD);
    }
    if (message.versionNumber !== 0) {
      writer.uint32(24).uint32(message.versionNumber);
    }
    if (message.batteryLevel !== 0) {
      writer.uint32(32).uint32(message.batteryLevel);
    }
    if (message.gaugeVariant !== 0) {
      writer.uint32(40).int32(message.gaugeVariant);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_GaugeInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_GaugeInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.serialNumber = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.gaugeUD = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.versionNumber = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.batteryLevel = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.gaugeVariant = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_GaugeInfo {
    return {
      serialNumber: isSet(object.serialNumber) ? globalThis.Number(object.serialNumber) : 0,
      gaugeUD: isSet(object.gaugeUD) ? globalThis.Number(object.gaugeUD) : 0,
      versionNumber: isSet(object.versionNumber) ? globalThis.Number(object.versionNumber) : 0,
      batteryLevel: isSet(object.batteryLevel) ? globalThis.Number(object.batteryLevel) : 0,
      gaugeVariant: isSet(object.gaugeVariant) ? gaugeVariantFromJSON(object.gaugeVariant) : 0,
    };
  },

  toJSON(message: Message_GaugeInfo): unknown {
    const obj: any = {};
    if (message.serialNumber !== 0) {
      obj.serialNumber = Math.round(message.serialNumber);
    }
    if (message.gaugeUD !== 0) {
      obj.gaugeUD = Math.round(message.gaugeUD);
    }
    if (message.versionNumber !== 0) {
      obj.versionNumber = Math.round(message.versionNumber);
    }
    if (message.batteryLevel !== 0) {
      obj.batteryLevel = Math.round(message.batteryLevel);
    }
    if (message.gaugeVariant !== 0) {
      obj.gaugeVariant = gaugeVariantToJSON(message.gaugeVariant);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_GaugeInfo>, I>>(base?: I): Message_GaugeInfo {
    return Message_GaugeInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_GaugeInfo>, I>>(object: I): Message_GaugeInfo {
    const message = createBaseMessage_GaugeInfo();
    message.serialNumber = object.serialNumber ?? 0;
    message.gaugeUD = object.gaugeUD ?? 0;
    message.versionNumber = object.versionNumber ?? 0;
    message.batteryLevel = object.batteryLevel ?? 0;
    message.gaugeVariant = object.gaugeVariant ?? 0;
    return message;
  },
};

function createBaseMessage_RecordList(): Message_RecordList {
  return { items: [] };
}

export const Message_RecordList: MessageFns<Message_RecordList> = {
  encode(message: Message_RecordList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      Message_RecordList_Item.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_RecordList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_RecordList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(Message_RecordList_Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_RecordList {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => Message_RecordList_Item.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Message_RecordList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Message_RecordList_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_RecordList>, I>>(base?: I): Message_RecordList {
    return Message_RecordList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_RecordList>, I>>(object: I): Message_RecordList {
    const message = createBaseMessage_RecordList();
    message.items = object.items?.map((e) => Message_RecordList_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessage_RecordList_Item(): Message_RecordList_Item {
  return {
    name: "",
    key: "",
    fileSize: 0,
    created: undefined,
    updated: undefined,
    recordType: 0,
    numPointsRequired: 0,
    numPointsTaken: 0,
  };
}

export const Message_RecordList_Item: MessageFns<Message_RecordList_Item> = {
  encode(message: Message_RecordList_Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.fileSize !== 0) {
      writer.uint32(24).uint32(message.fileSize);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(34).fork()).join();
    }
    if (message.updated !== undefined) {
      Timestamp.encode(toTimestamp(message.updated), writer.uint32(42).fork()).join();
    }
    if (message.recordType !== 0) {
      writer.uint32(48).int32(message.recordType);
    }
    if (message.numPointsRequired !== 0) {
      writer.uint32(56).uint32(message.numPointsRequired);
    }
    if (message.numPointsTaken !== 0) {
      writer.uint32(64).uint32(message.numPointsTaken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_RecordList_Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_RecordList_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fileSize = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updated = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.recordType = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.numPointsRequired = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.numPointsTaken = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_RecordList_Item {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      fileSize: isSet(object.fileSize) ? globalThis.Number(object.fileSize) : 0,
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
      updated: isSet(object.updated) ? fromJsonTimestamp(object.updated) : undefined,
      recordType: isSet(object.recordType) ? recordTypeFromJSON(object.recordType) : 0,
      numPointsRequired: isSet(object.numPointsRequired) ? globalThis.Number(object.numPointsRequired) : 0,
      numPointsTaken: isSet(object.numPointsTaken) ? globalThis.Number(object.numPointsTaken) : 0,
    };
  },

  toJSON(message: Message_RecordList_Item): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    if (message.updated !== undefined) {
      obj.updated = message.updated.toISOString();
    }
    if (message.recordType !== 0) {
      obj.recordType = recordTypeToJSON(message.recordType);
    }
    if (message.numPointsRequired !== 0) {
      obj.numPointsRequired = Math.round(message.numPointsRequired);
    }
    if (message.numPointsTaken !== 0) {
      obj.numPointsTaken = Math.round(message.numPointsTaken);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_RecordList_Item>, I>>(base?: I): Message_RecordList_Item {
    return Message_RecordList_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_RecordList_Item>, I>>(object: I): Message_RecordList_Item {
    const message = createBaseMessage_RecordList_Item();
    message.name = object.name ?? "";
    message.key = object.key ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.created = object.created ?? undefined;
    message.updated = object.updated ?? undefined;
    message.recordType = object.recordType ?? 0;
    message.numPointsRequired = object.numPointsRequired ?? 0;
    message.numPointsTaken = object.numPointsTaken ?? 0;
    return message;
  },
};

function createBaseMessage_Record(): Message_Record {
  return {
    recordID: 0,
    name: "",
    location: "",
    surveyor: "",
    key: "",
    created: undefined,
    updated: undefined,
    recordType: 0,
    numPointsRequired: 0,
    numPointsTaken: 0,
  };
}

export const Message_Record: MessageFns<Message_Record> = {
  encode(message: Message_Record, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recordID !== 0) {
      writer.uint32(8).uint32(message.recordID);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.location !== "") {
      writer.uint32(26).string(message.location);
    }
    if (message.surveyor !== "") {
      writer.uint32(34).string(message.surveyor);
    }
    if (message.key !== "") {
      writer.uint32(42).string(message.key);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(50).fork()).join();
    }
    if (message.updated !== undefined) {
      Timestamp.encode(toTimestamp(message.updated), writer.uint32(58).fork()).join();
    }
    if (message.recordType !== 0) {
      writer.uint32(64).int32(message.recordType);
    }
    if (message.numPointsRequired !== 0) {
      writer.uint32(72).uint32(message.numPointsRequired);
    }
    if (message.numPointsTaken !== 0) {
      writer.uint32(80).uint32(message.numPointsTaken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_Record {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_Record();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.recordID = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.location = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.surveyor = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updated = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.recordType = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.numPointsRequired = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.numPointsTaken = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_Record {
    return {
      recordID: isSet(object.recordID) ? globalThis.Number(object.recordID) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      location: isSet(object.location) ? globalThis.String(object.location) : "",
      surveyor: isSet(object.surveyor) ? globalThis.String(object.surveyor) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
      updated: isSet(object.updated) ? fromJsonTimestamp(object.updated) : undefined,
      recordType: isSet(object.recordType) ? recordTypeFromJSON(object.recordType) : 0,
      numPointsRequired: isSet(object.numPointsRequired) ? globalThis.Number(object.numPointsRequired) : 0,
      numPointsTaken: isSet(object.numPointsTaken) ? globalThis.Number(object.numPointsTaken) : 0,
    };
  },

  toJSON(message: Message_Record): unknown {
    const obj: any = {};
    if (message.recordID !== 0) {
      obj.recordID = Math.round(message.recordID);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.location !== "") {
      obj.location = message.location;
    }
    if (message.surveyor !== "") {
      obj.surveyor = message.surveyor;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    if (message.updated !== undefined) {
      obj.updated = message.updated.toISOString();
    }
    if (message.recordType !== 0) {
      obj.recordType = recordTypeToJSON(message.recordType);
    }
    if (message.numPointsRequired !== 0) {
      obj.numPointsRequired = Math.round(message.numPointsRequired);
    }
    if (message.numPointsTaken !== 0) {
      obj.numPointsTaken = Math.round(message.numPointsTaken);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_Record>, I>>(base?: I): Message_Record {
    return Message_Record.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_Record>, I>>(object: I): Message_Record {
    const message = createBaseMessage_Record();
    message.recordID = object.recordID ?? 0;
    message.name = object.name ?? "";
    message.location = object.location ?? "";
    message.surveyor = object.surveyor ?? "";
    message.key = object.key ?? "";
    message.created = object.created ?? undefined;
    message.updated = object.updated ?? undefined;
    message.recordType = object.recordType ?? 0;
    message.numPointsRequired = object.numPointsRequired ?? 0;
    message.numPointsTaken = object.numPointsTaken ?? 0;
    return message;
  },
};

function createBaseMessage_RecordPoint(): Message_RecordPoint {
  return {
    recordID: 0,
    name: "",
    key: 0,
    state: 0,
    method: 0,
    uom: 0,
    thickness: 0,
    velocity: 0,
    probeType: 0,
    UTMode: 0,
    taken: undefined,
    colNumX: 0,
    rowNumY: 0,
    ascan: undefined,
  };
}

export const Message_RecordPoint: MessageFns<Message_RecordPoint> = {
  encode(message: Message_RecordPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recordID !== 0) {
      writer.uint32(8).uint32(message.recordID);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.key !== 0) {
      writer.uint32(24).uint32(message.key);
    }
    if (message.state !== 0) {
      writer.uint32(32).int32(message.state);
    }
    if (message.method !== 0) {
      writer.uint32(40).int32(message.method);
    }
    if (message.uom !== 0) {
      writer.uint32(48).int32(message.uom);
    }
    if (message.thickness !== 0) {
      writer.uint32(56).uint32(message.thickness);
    }
    if (message.velocity !== 0) {
      writer.uint32(64).uint32(message.velocity);
    }
    if (message.probeType !== 0) {
      writer.uint32(72).int32(message.probeType);
    }
    if (message.UTMode !== 0) {
      writer.uint32(80).int32(message.UTMode);
    }
    if (message.taken !== undefined) {
      Timestamp.encode(toTimestamp(message.taken), writer.uint32(90).fork()).join();
    }
    if (message.colNumX !== 0) {
      writer.uint32(96).uint32(message.colNumX);
    }
    if (message.rowNumY !== 0) {
      writer.uint32(104).uint32(message.rowNumY);
    }
    if (message.ascan !== undefined) {
      AScan.encode(message.ascan, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_RecordPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_RecordPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.recordID = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.method = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.uom = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.thickness = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.velocity = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.probeType = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.UTMode = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.taken = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.colNumX = reader.uint32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.rowNumY = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.ascan = AScan.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_RecordPoint {
    return {
      recordID: isSet(object.recordID) ? globalThis.Number(object.recordID) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      state: isSet(object.state) ? measurementPointStateFromJSON(object.state) : 0,
      method: isSet(object.method) ? methodFromJSON(object.method) : 0,
      uom: isSet(object.uom) ? uOMFromJSON(object.uom) : 0,
      thickness: isSet(object.thickness) ? globalThis.Number(object.thickness) : 0,
      velocity: isSet(object.velocity) ? globalThis.Number(object.velocity) : 0,
      probeType: isSet(object.probeType) ? probeTypeFromJSON(object.probeType) : 0,
      UTMode: isSet(object.UTMode) ? uTModeFromJSON(object.UTMode) : 0,
      taken: isSet(object.taken) ? fromJsonTimestamp(object.taken) : undefined,
      colNumX: isSet(object.colNumX) ? globalThis.Number(object.colNumX) : 0,
      rowNumY: isSet(object.rowNumY) ? globalThis.Number(object.rowNumY) : 0,
      ascan: isSet(object.ascan) ? AScan.fromJSON(object.ascan) : undefined,
    };
  },

  toJSON(message: Message_RecordPoint): unknown {
    const obj: any = {};
    if (message.recordID !== 0) {
      obj.recordID = Math.round(message.recordID);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.state !== 0) {
      obj.state = measurementPointStateToJSON(message.state);
    }
    if (message.method !== 0) {
      obj.method = methodToJSON(message.method);
    }
    if (message.uom !== 0) {
      obj.uom = uOMToJSON(message.uom);
    }
    if (message.thickness !== 0) {
      obj.thickness = Math.round(message.thickness);
    }
    if (message.velocity !== 0) {
      obj.velocity = Math.round(message.velocity);
    }
    if (message.probeType !== 0) {
      obj.probeType = probeTypeToJSON(message.probeType);
    }
    if (message.UTMode !== 0) {
      obj.UTMode = uTModeToJSON(message.UTMode);
    }
    if (message.taken !== undefined) {
      obj.taken = message.taken.toISOString();
    }
    if (message.colNumX !== 0) {
      obj.colNumX = Math.round(message.colNumX);
    }
    if (message.rowNumY !== 0) {
      obj.rowNumY = Math.round(message.rowNumY);
    }
    if (message.ascan !== undefined) {
      obj.ascan = AScan.toJSON(message.ascan);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_RecordPoint>, I>>(base?: I): Message_RecordPoint {
    return Message_RecordPoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_RecordPoint>, I>>(object: I): Message_RecordPoint {
    const message = createBaseMessage_RecordPoint();
    message.recordID = object.recordID ?? 0;
    message.name = object.name ?? "";
    message.key = object.key ?? 0;
    message.state = object.state ?? 0;
    message.method = object.method ?? 0;
    message.uom = object.uom ?? 0;
    message.thickness = object.thickness ?? 0;
    message.velocity = object.velocity ?? 0;
    message.probeType = object.probeType ?? 0;
    message.UTMode = object.UTMode ?? 0;
    message.taken = object.taken ?? undefined;
    message.colNumX = object.colNumX ?? 0;
    message.rowNumY = object.rowNumY ?? 0;
    message.ascan = (object.ascan !== undefined && object.ascan !== null) ? AScan.fromPartial(object.ascan) : undefined;
    return message;
  },
};

function createBaseMessage_BScanList(): Message_BScanList {
  return { items: [] };
}

export const Message_BScanList: MessageFns<Message_BScanList> = {
  encode(message: Message_BScanList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      Message_BScanList_Item.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_BScanList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_BScanList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(Message_BScanList_Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_BScanList {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => Message_BScanList_Item.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Message_BScanList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Message_BScanList_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_BScanList>, I>>(base?: I): Message_BScanList {
    return Message_BScanList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_BScanList>, I>>(object: I): Message_BScanList {
    const message = createBaseMessage_BScanList();
    message.items = object.items?.map((e) => Message_BScanList_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessage_BScanList_Item(): Message_BScanList_Item {
  return { name: "", key: "", fileSize: 0, updated: undefined, numScanPoints: 0 };
}

export const Message_BScanList_Item: MessageFns<Message_BScanList_Item> = {
  encode(message: Message_BScanList_Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.fileSize !== 0) {
      writer.uint32(24).uint32(message.fileSize);
    }
    if (message.updated !== undefined) {
      Timestamp.encode(toTimestamp(message.updated), writer.uint32(34).fork()).join();
    }
    if (message.numScanPoints !== 0) {
      writer.uint32(48).uint32(message.numScanPoints);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_BScanList_Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_BScanList_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fileSize = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updated = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.numScanPoints = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_BScanList_Item {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      fileSize: isSet(object.fileSize) ? globalThis.Number(object.fileSize) : 0,
      updated: isSet(object.updated) ? fromJsonTimestamp(object.updated) : undefined,
      numScanPoints: isSet(object.numScanPoints) ? globalThis.Number(object.numScanPoints) : 0,
    };
  },

  toJSON(message: Message_BScanList_Item): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.updated !== undefined) {
      obj.updated = message.updated.toISOString();
    }
    if (message.numScanPoints !== 0) {
      obj.numScanPoints = Math.round(message.numScanPoints);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_BScanList_Item>, I>>(base?: I): Message_BScanList_Item {
    return Message_BScanList_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_BScanList_Item>, I>>(object: I): Message_BScanList_Item {
    const message = createBaseMessage_BScanList_Item();
    message.name = object.name ?? "";
    message.key = object.key ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.updated = object.updated ?? undefined;
    message.numScanPoints = object.numScanPoints ?? 0;
    return message;
  },
};

function createBaseMessage_BScan(): Message_BScan {
  return { BScanID: 0, name: "", key: "", updated: undefined, numScanPoints: 0, scanInterval: 0 };
}

export const Message_BScan: MessageFns<Message_BScan> = {
  encode(message: Message_BScan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.BScanID !== 0) {
      writer.uint32(8).uint32(message.BScanID);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.key !== "") {
      writer.uint32(26).string(message.key);
    }
    if (message.updated !== undefined) {
      Timestamp.encode(toTimestamp(message.updated), writer.uint32(34).fork()).join();
    }
    if (message.numScanPoints !== 0) {
      writer.uint32(40).uint32(message.numScanPoints);
    }
    if (message.scanInterval !== 0) {
      writer.uint32(48).uint32(message.scanInterval);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_BScan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_BScan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.BScanID = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updated = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.numScanPoints = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.scanInterval = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_BScan {
    return {
      BScanID: isSet(object.BScanID) ? globalThis.Number(object.BScanID) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      updated: isSet(object.updated) ? fromJsonTimestamp(object.updated) : undefined,
      numScanPoints: isSet(object.numScanPoints) ? globalThis.Number(object.numScanPoints) : 0,
      scanInterval: isSet(object.scanInterval) ? globalThis.Number(object.scanInterval) : 0,
    };
  },

  toJSON(message: Message_BScan): unknown {
    const obj: any = {};
    if (message.BScanID !== 0) {
      obj.BScanID = Math.round(message.BScanID);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.updated !== undefined) {
      obj.updated = message.updated.toISOString();
    }
    if (message.numScanPoints !== 0) {
      obj.numScanPoints = Math.round(message.numScanPoints);
    }
    if (message.scanInterval !== 0) {
      obj.scanInterval = Math.round(message.scanInterval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_BScan>, I>>(base?: I): Message_BScan {
    return Message_BScan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_BScan>, I>>(object: I): Message_BScan {
    const message = createBaseMessage_BScan();
    message.BScanID = object.BScanID ?? 0;
    message.name = object.name ?? "";
    message.key = object.key ?? "";
    message.updated = object.updated ?? undefined;
    message.numScanPoints = object.numScanPoints ?? 0;
    message.scanInterval = object.scanInterval ?? 0;
    return message;
  },
};

function createBaseMessage_BScanPoint(): Message_BScanPoint {
  return { BScanID: 0, uom: 0, thickness: 0, velocity: 0, probeType: 0, UTMode: 0, scanPointNum: 0, ascan: undefined };
}

export const Message_BScanPoint: MessageFns<Message_BScanPoint> = {
  encode(message: Message_BScanPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.BScanID !== 0) {
      writer.uint32(8).uint32(message.BScanID);
    }
    if (message.uom !== 0) {
      writer.uint32(16).int32(message.uom);
    }
    if (message.thickness !== 0) {
      writer.uint32(24).uint32(message.thickness);
    }
    if (message.velocity !== 0) {
      writer.uint32(32).uint32(message.velocity);
    }
    if (message.probeType !== 0) {
      writer.uint32(40).int32(message.probeType);
    }
    if (message.UTMode !== 0) {
      writer.uint32(48).int32(message.UTMode);
    }
    if (message.scanPointNum !== 0) {
      writer.uint32(56).uint32(message.scanPointNum);
    }
    if (message.ascan !== undefined) {
      AScan.encode(message.ascan, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message_BScanPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage_BScanPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.BScanID = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.uom = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.thickness = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.velocity = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.probeType = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.UTMode = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.scanPointNum = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.ascan = AScan.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message_BScanPoint {
    return {
      BScanID: isSet(object.BScanID) ? globalThis.Number(object.BScanID) : 0,
      uom: isSet(object.uom) ? uOMFromJSON(object.uom) : 0,
      thickness: isSet(object.thickness) ? globalThis.Number(object.thickness) : 0,
      velocity: isSet(object.velocity) ? globalThis.Number(object.velocity) : 0,
      probeType: isSet(object.probeType) ? probeTypeFromJSON(object.probeType) : 0,
      UTMode: isSet(object.UTMode) ? uTModeFromJSON(object.UTMode) : 0,
      scanPointNum: isSet(object.scanPointNum) ? globalThis.Number(object.scanPointNum) : 0,
      ascan: isSet(object.ascan) ? AScan.fromJSON(object.ascan) : undefined,
    };
  },

  toJSON(message: Message_BScanPoint): unknown {
    const obj: any = {};
    if (message.BScanID !== 0) {
      obj.BScanID = Math.round(message.BScanID);
    }
    if (message.uom !== 0) {
      obj.uom = uOMToJSON(message.uom);
    }
    if (message.thickness !== 0) {
      obj.thickness = Math.round(message.thickness);
    }
    if (message.velocity !== 0) {
      obj.velocity = Math.round(message.velocity);
    }
    if (message.probeType !== 0) {
      obj.probeType = probeTypeToJSON(message.probeType);
    }
    if (message.UTMode !== 0) {
      obj.UTMode = uTModeToJSON(message.UTMode);
    }
    if (message.scanPointNum !== 0) {
      obj.scanPointNum = Math.round(message.scanPointNum);
    }
    if (message.ascan !== undefined) {
      obj.ascan = AScan.toJSON(message.ascan);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message_BScanPoint>, I>>(base?: I): Message_BScanPoint {
    return Message_BScanPoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message_BScanPoint>, I>>(object: I): Message_BScanPoint {
    const message = createBaseMessage_BScanPoint();
    message.BScanID = object.BScanID ?? 0;
    message.uom = object.uom ?? 0;
    message.thickness = object.thickness ?? 0;
    message.velocity = object.velocity ?? 0;
    message.probeType = object.probeType ?? 0;
    message.UTMode = object.UTMode ?? 0;
    message.scanPointNum = object.scanPointNum ?? 0;
    message.ascan = (object.ascan !== undefined && object.ascan !== null) ? AScan.fromPartial(object.ascan) : undefined;
    return message;
  },
};

function createBaseNotifyLiveMeasurement(): NotifyLiveMeasurement {
  return {
    liveMeasurementType: 0,
    index: 0,
    statusBits: 0,
    UTMode: 0,
    thickness: 0,
    velocity: 0,
    gaindB: 0,
    surfaceTemp: 0,
    batteryLevel: 0,
  };
}

export const NotifyLiveMeasurement: MessageFns<NotifyLiveMeasurement> = {
  encode(message: NotifyLiveMeasurement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.liveMeasurementType !== 0) {
      writer.uint32(8).int32(message.liveMeasurementType);
    }
    if (message.index !== 0) {
      writer.uint32(16).uint32(message.index);
    }
    if (message.statusBits !== 0) {
      writer.uint32(24).uint32(message.statusBits);
    }
    if (message.UTMode !== 0) {
      writer.uint32(32).int32(message.UTMode);
    }
    if (message.thickness !== 0) {
      writer.uint32(40).uint32(message.thickness);
    }
    if (message.velocity !== 0) {
      writer.uint32(48).uint32(message.velocity);
    }
    if (message.gaindB !== 0) {
      writer.uint32(56).uint32(message.gaindB);
    }
    if (message.surfaceTemp !== 0) {
      writer.uint32(64).uint32(message.surfaceTemp);
    }
    if (message.batteryLevel !== 0) {
      writer.uint32(72).uint32(message.batteryLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotifyLiveMeasurement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotifyLiveMeasurement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.liveMeasurementType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.statusBits = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.UTMode = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.thickness = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.velocity = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.gaindB = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.surfaceTemp = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.batteryLevel = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotifyLiveMeasurement {
    return {
      liveMeasurementType: isSet(object.liveMeasurementType)
        ? liveMeasurementTypeFromJSON(object.liveMeasurementType)
        : 0,
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      statusBits: isSet(object.statusBits) ? globalThis.Number(object.statusBits) : 0,
      UTMode: isSet(object.UTMode) ? uTModeFromJSON(object.UTMode) : 0,
      thickness: isSet(object.thickness) ? globalThis.Number(object.thickness) : 0,
      velocity: isSet(object.velocity) ? globalThis.Number(object.velocity) : 0,
      gaindB: isSet(object.gaindB) ? globalThis.Number(object.gaindB) : 0,
      surfaceTemp: isSet(object.surfaceTemp) ? globalThis.Number(object.surfaceTemp) : 0,
      batteryLevel: isSet(object.batteryLevel) ? globalThis.Number(object.batteryLevel) : 0,
    };
  },

  toJSON(message: NotifyLiveMeasurement): unknown {
    const obj: any = {};
    if (message.liveMeasurementType !== 0) {
      obj.liveMeasurementType = liveMeasurementTypeToJSON(message.liveMeasurementType);
    }
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.statusBits !== 0) {
      obj.statusBits = Math.round(message.statusBits);
    }
    if (message.UTMode !== 0) {
      obj.UTMode = uTModeToJSON(message.UTMode);
    }
    if (message.thickness !== 0) {
      obj.thickness = Math.round(message.thickness);
    }
    if (message.velocity !== 0) {
      obj.velocity = Math.round(message.velocity);
    }
    if (message.gaindB !== 0) {
      obj.gaindB = Math.round(message.gaindB);
    }
    if (message.surfaceTemp !== 0) {
      obj.surfaceTemp = Math.round(message.surfaceTemp);
    }
    if (message.batteryLevel !== 0) {
      obj.batteryLevel = Math.round(message.batteryLevel);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotifyLiveMeasurement>, I>>(base?: I): NotifyLiveMeasurement {
    return NotifyLiveMeasurement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotifyLiveMeasurement>, I>>(object: I): NotifyLiveMeasurement {
    const message = createBaseNotifyLiveMeasurement();
    message.liveMeasurementType = object.liveMeasurementType ?? 0;
    message.index = object.index ?? 0;
    message.statusBits = object.statusBits ?? 0;
    message.UTMode = object.UTMode ?? 0;
    message.thickness = object.thickness ?? 0;
    message.velocity = object.velocity ?? 0;
    message.gaindB = object.gaindB ?? 0;
    message.surfaceTemp = object.surfaceTemp ?? 0;
    message.batteryLevel = object.batteryLevel ?? 0;
    return message;
  },
};

function createBaseFrozenLiveMeasurement(): FrozenLiveMeasurement {
  return {
    index: 0,
    serialNumber: 0,
    gaugeUD: 0,
    batteryLevel: 0,
    validMeasurement: false,
    stableMeasurement: false,
    uom: 0,
    thickness: 0,
    velocity: 0,
    probeType: 0,
    UTMode: 0,
    gaindB: 0,
    surfaceTemp: 0,
    ascan: undefined,
  };
}

export const FrozenLiveMeasurement: MessageFns<FrozenLiveMeasurement> = {
  encode(message: FrozenLiveMeasurement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.serialNumber !== 0) {
      writer.uint32(16).uint32(message.serialNumber);
    }
    if (message.gaugeUD !== 0) {
      writer.uint32(24).uint32(message.gaugeUD);
    }
    if (message.batteryLevel !== 0) {
      writer.uint32(32).uint32(message.batteryLevel);
    }
    if (message.validMeasurement !== false) {
      writer.uint32(40).bool(message.validMeasurement);
    }
    if (message.stableMeasurement !== false) {
      writer.uint32(48).bool(message.stableMeasurement);
    }
    if (message.uom !== 0) {
      writer.uint32(56).int32(message.uom);
    }
    if (message.thickness !== 0) {
      writer.uint32(64).uint32(message.thickness);
    }
    if (message.velocity !== 0) {
      writer.uint32(72).uint32(message.velocity);
    }
    if (message.probeType !== 0) {
      writer.uint32(80).int32(message.probeType);
    }
    if (message.UTMode !== 0) {
      writer.uint32(88).int32(message.UTMode);
    }
    if (message.gaindB !== 0) {
      writer.uint32(96).uint32(message.gaindB);
    }
    if (message.surfaceTemp !== 0) {
      writer.uint32(104).uint32(message.surfaceTemp);
    }
    if (message.ascan !== undefined) {
      AScan.encode(message.ascan, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FrozenLiveMeasurement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFrozenLiveMeasurement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serialNumber = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.gaugeUD = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.batteryLevel = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.validMeasurement = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.stableMeasurement = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.uom = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.thickness = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.velocity = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.probeType = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.UTMode = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.gaindB = reader.uint32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.surfaceTemp = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.ascan = AScan.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FrozenLiveMeasurement {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      serialNumber: isSet(object.serialNumber) ? globalThis.Number(object.serialNumber) : 0,
      gaugeUD: isSet(object.gaugeUD) ? globalThis.Number(object.gaugeUD) : 0,
      batteryLevel: isSet(object.batteryLevel) ? globalThis.Number(object.batteryLevel) : 0,
      validMeasurement: isSet(object.validMeasurement) ? globalThis.Boolean(object.validMeasurement) : false,
      stableMeasurement: isSet(object.stableMeasurement) ? globalThis.Boolean(object.stableMeasurement) : false,
      uom: isSet(object.uom) ? uOMFromJSON(object.uom) : 0,
      thickness: isSet(object.thickness) ? globalThis.Number(object.thickness) : 0,
      velocity: isSet(object.velocity) ? globalThis.Number(object.velocity) : 0,
      probeType: isSet(object.probeType) ? probeTypeFromJSON(object.probeType) : 0,
      UTMode: isSet(object.UTMode) ? uTModeFromJSON(object.UTMode) : 0,
      gaindB: isSet(object.gaindB) ? globalThis.Number(object.gaindB) : 0,
      surfaceTemp: isSet(object.surfaceTemp) ? globalThis.Number(object.surfaceTemp) : 0,
      ascan: isSet(object.ascan) ? AScan.fromJSON(object.ascan) : undefined,
    };
  },

  toJSON(message: FrozenLiveMeasurement): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.serialNumber !== 0) {
      obj.serialNumber = Math.round(message.serialNumber);
    }
    if (message.gaugeUD !== 0) {
      obj.gaugeUD = Math.round(message.gaugeUD);
    }
    if (message.batteryLevel !== 0) {
      obj.batteryLevel = Math.round(message.batteryLevel);
    }
    if (message.validMeasurement !== false) {
      obj.validMeasurement = message.validMeasurement;
    }
    if (message.stableMeasurement !== false) {
      obj.stableMeasurement = message.stableMeasurement;
    }
    if (message.uom !== 0) {
      obj.uom = uOMToJSON(message.uom);
    }
    if (message.thickness !== 0) {
      obj.thickness = Math.round(message.thickness);
    }
    if (message.velocity !== 0) {
      obj.velocity = Math.round(message.velocity);
    }
    if (message.probeType !== 0) {
      obj.probeType = probeTypeToJSON(message.probeType);
    }
    if (message.UTMode !== 0) {
      obj.UTMode = uTModeToJSON(message.UTMode);
    }
    if (message.gaindB !== 0) {
      obj.gaindB = Math.round(message.gaindB);
    }
    if (message.surfaceTemp !== 0) {
      obj.surfaceTemp = Math.round(message.surfaceTemp);
    }
    if (message.ascan !== undefined) {
      obj.ascan = AScan.toJSON(message.ascan);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FrozenLiveMeasurement>, I>>(base?: I): FrozenLiveMeasurement {
    return FrozenLiveMeasurement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FrozenLiveMeasurement>, I>>(object: I): FrozenLiveMeasurement {
    const message = createBaseFrozenLiveMeasurement();
    message.index = object.index ?? 0;
    message.serialNumber = object.serialNumber ?? 0;
    message.gaugeUD = object.gaugeUD ?? 0;
    message.batteryLevel = object.batteryLevel ?? 0;
    message.validMeasurement = object.validMeasurement ?? false;
    message.stableMeasurement = object.stableMeasurement ?? false;
    message.uom = object.uom ?? 0;
    message.thickness = object.thickness ?? 0;
    message.velocity = object.velocity ?? 0;
    message.probeType = object.probeType ?? 0;
    message.UTMode = object.UTMode ?? 0;
    message.gaindB = object.gaindB ?? 0;
    message.surfaceTemp = object.surfaceTemp ?? 0;
    message.ascan = (object.ascan !== undefined && object.ascan !== null) ? AScan.fromPartial(object.ascan) : undefined;
    return message;
  },
};

function createBaseAScan(): AScan {
  return { rectify: 0, ascanStart: 0, ascanWidth: 0, ascanPoints: new Uint8Array(0), echo1: 0, echo2: 0, echo3: 0 };
}

export const AScan: MessageFns<AScan> = {
  encode(message: AScan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rectify !== 0) {
      writer.uint32(8).int32(message.rectify);
    }
    if (message.ascanStart !== 0) {
      writer.uint32(16).uint32(message.ascanStart);
    }
    if (message.ascanWidth !== 0) {
      writer.uint32(24).uint32(message.ascanWidth);
    }
    if (message.ascanPoints.length !== 0) {
      writer.uint32(34).bytes(message.ascanPoints);
    }
    if (message.echo1 !== 0) {
      writer.uint32(40).uint32(message.echo1);
    }
    if (message.echo2 !== 0) {
      writer.uint32(48).uint32(message.echo2);
    }
    if (message.echo3 !== 0) {
      writer.uint32(56).uint32(message.echo3);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AScan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAScan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rectify = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ascanStart = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ascanWidth = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ascanPoints = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.echo1 = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.echo2 = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.echo3 = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AScan {
    return {
      rectify: isSet(object.rectify) ? aScanRectifyFromJSON(object.rectify) : 0,
      ascanStart: isSet(object.ascanStart) ? globalThis.Number(object.ascanStart) : 0,
      ascanWidth: isSet(object.ascanWidth) ? globalThis.Number(object.ascanWidth) : 0,
      ascanPoints: isSet(object.ascanPoints) ? bytesFromBase64(object.ascanPoints) : new Uint8Array(0),
      echo1: isSet(object.echo1) ? globalThis.Number(object.echo1) : 0,
      echo2: isSet(object.echo2) ? globalThis.Number(object.echo2) : 0,
      echo3: isSet(object.echo3) ? globalThis.Number(object.echo3) : 0,
    };
  },

  toJSON(message: AScan): unknown {
    const obj: any = {};
    if (message.rectify !== 0) {
      obj.rectify = aScanRectifyToJSON(message.rectify);
    }
    if (message.ascanStart !== 0) {
      obj.ascanStart = Math.round(message.ascanStart);
    }
    if (message.ascanWidth !== 0) {
      obj.ascanWidth = Math.round(message.ascanWidth);
    }
    if (message.ascanPoints.length !== 0) {
      obj.ascanPoints = base64FromBytes(message.ascanPoints);
    }
    if (message.echo1 !== 0) {
      obj.echo1 = Math.round(message.echo1);
    }
    if (message.echo2 !== 0) {
      obj.echo2 = Math.round(message.echo2);
    }
    if (message.echo3 !== 0) {
      obj.echo3 = Math.round(message.echo3);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AScan>, I>>(base?: I): AScan {
    return AScan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AScan>, I>>(object: I): AScan {
    const message = createBaseAScan();
    message.rectify = object.rectify ?? 0;
    message.ascanStart = object.ascanStart ?? 0;
    message.ascanWidth = object.ascanWidth ?? 0;
    message.ascanPoints = object.ascanPoints ?? new Uint8Array(0);
    message.echo1 = object.echo1 ?? 0;
    message.echo2 = object.echo2 ?? 0;
    message.echo3 = object.echo3 ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
